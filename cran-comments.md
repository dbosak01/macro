# macro v0.1.5

* Several examples are displaying the contents of R files that are executed
by the package function msource(). It is important that the user sees
and understands the code in the file.  So I'm including that code
as comments in the example so it is perfectly clear to the user
what is happening.  
